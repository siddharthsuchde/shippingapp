<%= render 'shared/content_title', title: 'Create Shipment' %>

<%= form_for [@order, @shipment] do |f| %>
  <%= f.label :service_type %>
  <%= f.text_field :service_type %>
  <%= f.label :weight %>
  <%= f.text_field :weight %> 
<p>
  <p>
  <%= f.submit "Create Shipment", class: 'btn btn-primary btn-success' %>
<% end %>