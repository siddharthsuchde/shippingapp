<ul class="nav">
  <li>
    <%= link_to "Home", root_path, :class => 'brand' %>
  </li>
</ul>
  
  <div class = "nav-item">
    <%= link_to "New Order", new_order_path, class: 'btn btn-success btn-small'%>
  </div>

 

<div class =  "nav-item">
  <% if logged_in? %>
    Welcome <%= current_user.username %>
    <%= link_to "Logout", logout_path, class: 'btn btn-small' %>
  <% else %>
    <%= link_to "Register", new_user_path, class: 'btn btn-small' %>
    <%= link_to "Login", login_path, class: 'btn btn-small'%>
  <% end %>
  
</div>

<div class = "nav-item">
  <%= link_to 'All Shipments', shipments_path, class: 'btn btn-small' %>
</div>
